# 需要人手点的检查清单

自动化能覆盖的部分已经写进 `test/`（单元测试，纯 Dart）与 `integration_test/`
（真机 SQL 与平台通道）。**下面这些必须有个人在场**：要么需要真实设备上的系统
界面，要么需要人的眼睛判断「看起来对不对」。

每做完一项就把方框改成 `[x]`，并写上日期与设备，便于回看是哪一版验的。

> 为什么要把这份清单单独写出来：指南 9 阶段 7 要求「无法验证的设备 / 格式
> 必须如实列为未验证」。把未验证的事情列清楚，比让它含糊地混在「已完成」里
> 诚实得多。

---

## 一、导入：系统文件选择器（当前最关键的未验证项）

这几项都没法自动化：选择器的界面随 Android 版本与厂商而变，脚本点它不可靠。

- [ ] **在系统选择器里真的选一份微信账单 XLSX**（微信现在导出 xlsx，不是 csv），
      看是否进到核对页，且笔数 / 金额与文件内容对得上
      —— 解析本身已用真文件验过，这里要验的是**选择器那一段**
- [ ] **在系统选择器里真的选一份支付宝账单**（支付宝的说明行更多，也是 xlsx）
- [ ] **选一份 GBK 编码的 CSV 账单**，确认没有乱码，且核对页上显示的编码是 GBK
- [ ] **选一个不是账单的文件**（例如一张图片）：应该明确报错，
      而不是"导入成功 0 笔"
- [ ] **在选择器里按返回 / 取消**：应安静地回到选文件页，不弹错误提示、
      按钮仍可点
- [ ] **选一个很大的文件**（> 64 MB）：应被挡下并说明上限
- [ ] **连续快速点两次「选择账单文件」**：只能开出一个选择器
- [ ] **选完文件后立刻切到别的应用再回来**：解析结果还在（或能重新开始），
      不出现白屏
- [ ] **界面上写着支持哪些格式**：来源页的「通用表格」副标题与选文件页的
      提示都应是 `CSV / XLS / XLSX`，而不是只写 CSV（本轮改的文案）
- [ ] **选一份真正的老式 `.xls`**（早年银行导出的那种二进制格式）：
      应当明确说「这是老版本的 Excel 文件（.xls）…请另存为 .xlsx 或 CSV」，
      **不能**变成一页乱码 + 字段映射页。⚠️ 注意区分：
      `test/账单/平安银行银行卡交易明细.xls` 是**改名的 xlsx**，
      这一类应当正常导入

### 提交之后：界面必须立刻跟上（本轮真机报的 bug，见 `DECISIONS.md` 68 节）

自动化已盖（`test/import_flow_widget_test.dart` 里那两条：
「提交之后首页与整理页立刻就能看到这批账单」与
「导入的是另一个月的账单时，界面跟着走到那个月」——
把接线去掉它们会变红），下面这条是拿**自己的真账单**再确认一遍：

- [ ] 把一份**本月之外的**账单（比如上个月的）导进去 → 点「开始整理」：
      界面应**自己跳到那个月**并列刚导入的记录，而不需要杀掉应用重开
- [ ] 导入完成后**不重启**直接回首页：金额、笔数、「本月账单」里的来源分组
      都应当是导入之后的数字
- [ ] 想验「撤回」那一边（同一个通知路径，一个不受益于选择器）：
      进「导入记录」→ 撤回一个批次 → 首页金额应当**立刻**降下来。
      ⚠️ 撤回会真的删掉那批交易，而且会**释放文件的读取授权**，
      之后要重新导入得再选一次文件

## 二、导入：字段映射与重复

- [ ] **导入一份表头认不出来的 CSV**（例如把列名改成"日期戳/摘要/数额"）：
      应进入字段映射页，且能看到文件前几行原文
- [ ] 映射页上**故意把同一列指给两个字段**：按钮应变灰并说明原因
- [ ] 映射页上**故意漏掉必填项**：同上
- [ ] **同一份文件导入两次**：第二次应提示这些记录已经导入过，
      且账本里的笔数不变（真机上验的是「同源重复不产生第二笔」）
- [ ] **导入一份没有交易单号的账单**，其中有两行同商户同时刻同金额：
      应逐组要求确认，**没说「完成」之前不能提交**
- [ ] 疑似重复选「只保留一笔」后提交：账本里只多一笔

## 三、导入：撤回

- [ ] 导入后立刻**撤回**：核对页 / 首页 / 月报的金额同步回落
- [ ] 导入后**先给某笔分好类，再撤回**：那一笔应被保留，弹窗里说明
      「保留 N 笔 —— 你已经给它们分过类」
- [ ] **同一笔消费出现在两份账单里**（例如手工造两份带同样交易单号的文件）：
      撤回其中一份，另一份引用的那笔必须留下
- [ ] 撤回后**再点一次撤回**：应安全返回"没有记录需要移除"，不报错
- [ ] **撤回一份账单，而其中某笔消费已被退款关联**（先导入 → 把某笔标成
      「收到退款」并关联它 → 再撤回）：确认框要说清「N 笔退款会解除关联
      并回到待整理（退款本身不会被删掉）」；撤回后那笔退款仍存在、回到待整理，
      原消费的月报净消费变回去

## 四、视觉与无障碍（阶段 7）

> 33 个设计状态的逐项走查已由需求方于 2026-09-25 完成（未提出问题）。
> 下面这几项**不在**那次走查的范围内 —— 它们分别是「六套主题逐页对比度」
> 「系统字号调到最大」「TalkBack」「横屏旋转」，仍然需要人手过。

- [ ] **六套主题逐一走查**：核对页、映射页、重复页、导入记录页的
      文字与背景对比度（导入这几页是阶段 3 新加的，还没在六套主题下看过）
- [ ] **把系统字号调到最大**（Android 上就是 200%）：主要页面不溢出，
      内容靠滚动看完；堆叠卡片会跟着变高，不是把文字挤出去
      （应用内的字号上限已去掉，见 `DECISIONS.md` 第 60 节）
- [ ] 大字号下再滑一次卡片：右滑确认、左滑稍后都要照旧生效
- [ ] 大字号下看引导页三屏：插画里的字**不放大**（它是装饰），
      标题与正文照旧放大，画面上不该出现红黄斜纹
- [ ] **开 TalkBack** 走一遍：选择文件、确认导入、逐组处理重复都能读出来
- [ ] **横屏 / 旋转**：核对页与映射页不崩、内容不重叠
- [ ] **系统返回键**：从核对页返回应回到选文件页且不丢暂存内容；
      从解析中返回应安全取消

## 五、权限与隐私（"承诺与实现一致"）

- [ ] 在系统「应用信息 → 权限」里确认：**没有**任何存储 / 文件访问权限，
      只有应用自己声明的那些
- [ ] 确认联网**只发生在检查更新时**：第一次打开时看系统流量统计，或把网络
      断开后用「我的 → 检查更新」确认它只是提示「连不上网络」而不会报错
- [ ] 「清除本地数据」之后：账本为空，**导入记录也一起清掉**

## 六、真机集中测试命令

一次跑完所有自动化测试（约 1 分钟，需要设备已连接）：

```powershell
Set-Location 'D:\dev\younum\app'
flutter analyze
flutter test
flutter test integration_test/database_test.dart -d 412913d4
flutter test integration_test/file_source_test.dart -d 412913d4
```

— — —

**⚠️ 改了原生代码（Kotlin / Manifest / Gradle）或依赖之后，上面四条不够。**
它们跑的是 debug 构建，**不跑 R8**。release 专属的崩溃（反射被裁、保持规则缺失）
只能这样发现：

```powershell
flutter build apk --release
# 装到真机上，**真的点开图标看一眼**，别只看安装输出里的 Success
```

上一轮就是靠这一步才发现了「release 包点开即退出」（见 `DECISIONS.md` 67 节）。

---

## 七、首次使用：三屏引导

自动化已经守住手感约定（首尾不循环、竖向滑动不翻页、不足 50dp 不翻页、
每屏都能跳过、按钮文案随屏变化），见 `test/onboarding_widget_test.dart`。
下面这些要靠眼睛和设备：

- [x] 真机逐屏核对三屏文案、插画、分页圆点（2026-09-25，2211133C）
- [x] 真机左滑前进 / 右滑返回（2026-09-25）
- [ ] **全新安装后第一次打开**就是引导页（升级安装不算，要 `adb uninstall` 再装）
- [ ] 走完引导后**再启动一次**：不再出现引导，直接进首页
- [ ] **把系统字号调到最大**：三屏都不溢出（每屏的文案框自己可以纵向滚动，
      但插画与页脚不能重叠）
- [ ] **开 TalkBack** 走一遍：每屏能听到「产品介绍，第 n 页，共 3 页」，
      分页圆点能听到「第 n 页，共 3 页，当前页」
- [ ] **开系统「减少动画」**：点圆点 / 下一步应直接换页，不做滑动动画
- [ ] **横屏**：三屏不崩、插画与文案不重叠

## 八、桌面图标

- [x] 真机桌面显示正常，未被裁切（2026-09-25，Android 16 / 2211133C）
- [ ] 换一个**圆形图标**的桌面主题（或长按图标选「编辑」）：圆形裁切下不裁到主体
- [ ] Android 13+ 开**主题图标**（跟随壁纸取色）：单色层能正确出形状
- [ ] 小尺寸（例如应用抽屉里的最小图标）下仍能认出是三张卡片 + 一片叶子

## 九、启动异常怎么分诊（环境 / 原生层）

真机上一片空白时，**先分清是「进程活着但画面不对」还是「进程已经不在了」**，
两类原因完全不同，查错方向也相反：

- [ ] `adb shell pidof com.younum.app` **有输出**（进程在）+ 白屏 → 渲染/窗口问题。
      再看 logcat 里有没有 `I/flutter` 的 Dart 输出：有说明 Dart 层已经跑起来了
      （能看到 `[启动]` 之类），问题在画面这一层
- [ ] `pidof` **没有输出**（进程不在）→ 原生层崩了，白屏和闪退提示都不会有。
      去 logcat 里搜 `AndroidRuntime` / `FATAL EXCEPTION`，栈里出现 `r8-map-id-`
      就是 release 专属的 R8 裁剪问题（见 `DECISIONS.md` 67 节）
- [ ] 已经实测过、**不成立**的一个旧猜测：本机 Impeller（Vulkan）会白屏。
      同一台机器上同一个 APK 用普通 `am start` 启动（Impeller 开着）画面完全正常，
      `--no-enable-impeller` 也正常。所以**不要**去加
      `io.flutter.embedding.android.EnableImpeller=false`
- [ ] MIUI 上安装被拦（`INSTALL_FAILED_USER_RESTRICTED`）：先
      `adb shell input keyevent KEYCODE_WAKEUP` + `adb shell svc power stayon true` 再重试

## 十、月报页：确认本月范围完整

自动化已覆盖「入口在不在、点了是不是真的写库、还有待整理时不给确认」
（`test/report_coverage_widget_test.dart`）。下面这些靠眼睛：

- [ ] 整理完一个月的记录后进月报页：应看到「我确认 X 月账单范围完整」按钮
- [ ] 点它：先弹一层问话，选「再看看」不改任何状态，再点一次选「确认范围完整」才生效
- [ ] 确认后：「部分账单」标记与提示消失，日均变成「按当月天数」，环比出现
- [ ] **还没整理完时**进月报页：应看到「还有 N 笔没有整理完」+「继续整理」，
      而**不是**范围确认按钮
- [ ] 整理完成页的确认勾选项里，月份应与当前选中的月份一致（切个月份再看一眼）
- [ ] 开 TalkBack：确认按钮与弹层都能读出来，焦点不会跑到弹层后面
## 十一、拆分与交易性质（阶段 4 新接线）

自动化已覆盖「页面展示的是传进来的那一笔、点了真的落库、合计不对不能提交、
排除统计没写原因不能提交、收到退款必须先选原消费、候选里没有这笔自己、
提交后落下关联、撤销能还原」（`test/split_test.dart`、
`test/split_widget_test.dart`、`test/nature_test.dart`、
`test/nature_widget_test.dart`、`test/detail_edit_test.dart`、
真机 `database_test.dart`）。下面这些靠人手：

- [ ] 从明细/详情页点「拆分这笔消费」：应看到**那一笔**的商户、日期与原始金额
- [ ] 拆分改到合计不符：按钮应保持灰的，「剩余」标记要说清差多少
- [ ] 拆分保存后回详情页：分类摘要应是拆分后的样子，月报总额不变
- [ ] 拆分后按「撤销」：应回到拆分前单个用途的样子
- [ ] 已拆过的记录再进拆分页：应回填原来的两项，而不是从零开始
- [ ] 详情页点「设为转账 / 收入 / 不计入」：应看到那一笔的商户/金额，且**没有**预选
- [ ] 选「暂不计入统计」不写原因：按钮应保持灰的并说明原因
- [ ] 标成转账后回整理页：这笔应从待整理里消失，月报消费额相应减少
- [ ] 标成转账后按「撤销」：性质、原因与用途都要回到原来的样子
- [ ] 选「收到退款」不选原消费：按钮应保持灰的，并说明「退款要关联到原消费」
- [ ] 点「选择原消费」：弹层里应是本月的消费，**且没有这笔自己**
- [ ] 选完原消费再确认：这笔的卡片应从待整理里消失，月报净消费相应减少
- [ ] 拿一笔上个月的、已归过类的消费，在本月的退款上选它：应能关联成功（跨月）
- [ ] 打开一笔**已关联**的退款（整理 → 右上「查看全部明细」→ 那笔退款 → 详情 →
      设为转账 / 收入 / 不计入）：应看到「收到退款」已选中、原消费已预填，
      并说清「这笔退款已经关联到…，要改先按下面的取消退款关联」
- [ ] 跨月关联的退款再打开这一页：原消费那一行要显示商户与金额，而不是「未知记录」
- [ ] 点「取消退款关联」：先问一句（并说明影响）；确认后这笔回到待整理，
      原消费的月报净消费变回去，退款金额不再被抵扣
- [ ] 找不到原消费时选「暂不计入统计」写原因：也应当能提交（这是给找回不着的退款留的路）
- [ ] 拿一笔**拆分过的消费**当原消费：选中后应出现「抵扣到哪几项」，每项显示原金额
- [ ] 那几项都填 0：按钮应保持灰的，并提示「拆成了多项…合计要精确等于退款金额」
- [ ] 两项填成合计不等于退款金额：仍不能提交；填对后提示消失、可以提交
- [ ] 汇总对比：关联成功后月报里那几个分类的净消费按你填的金额分别减少
- [ ] 拿一笔**拆分过、且某一项已被上一笔退款抵满**的消费：再抵那一项应被拒并说明原因

## 十二、分类管理（阶段 4 新接线）

自动化已覆盖「新建真的写进分类表、同级重名被拒且不落库、改图标只改图标、
界面失败不返回上一页」（`test/category_management_test.dart`、
`test/category_widget_test.dart`），真机 `database_test.dart` 里另有两条
（新建与改图标重开数据库仍在、同级重名不落库），2026-09-25 已跑过。
下面这些靠人手：

- [ ] 我的 → 分类管理：应看到库里**真实**的分类（内置 + 自定义的「宠物」「学习成长」）
- [ ] 新建一个分类（如「养花」）并选图标 → 保存后再进分类管理：它应在列表里
- [ ] **杀掉应用重新打开**：刚建的分类仍在（这是这一块的核心：以前重启就没）
- [ ] 整理一笔消费 → 选择用途：刚建的那个分类应出现在「我的分类」里，
      并且能真的选中、确认后分类名显示正确（以前界面列得出、库甲认不了）
- [ ] 在分类管理里给一个分类换图标：重新打开该页图标仍是新的；
      分类名、整理进度、金额都不变
- [ ] 改成与现有分类同名的名字：应提示「这个分类已经存在」
- [ ] 点「从图片选择」：打开系统图片选择器；取消应什么都不改
- [ ] 选一张横图：预览里已是正方形缩略图；保存后分类网格、分类管理、明细
      与月报里的图标都变成这张图
- [ ] 杀应用重开：图片图标仍在（它指向应用私有目录里的副本，原相册照片删了也不影响）
- [ ] 选一张超过 2 MB 的图：应提示「超过 2 MB，先压缩一下再试」，旧图标不变
- [ ] 连选两张图：只有最后一张生效（第一张的处理结果不得覆盖）
- [ ] 点「恢复默认图标」再保存：回到内置图标，且该图片资源被清理
- [ ] 打开一个**自建**分类 → 底部点「删除此分类」：弹层要说清「已归类的记录不受影响，
      只是不再出现在选择列表里」；确认后回到分类管理，底部出现「已归档」
- [ ] 再点那个分类的「恢复」：它回到分类列表，整理时又能选到它
- [ ] 打开一个**内置**分类：按钮应是「归档此分类」（不是删除）
- [ ] 把一个分类归档后回到**明细/月报**：用过它的那些记录仍显示它的名字与图标（
      这是「历史引用继续有效」的关键一条）
- [ ] 只剩一个一级分类时打开它：「归档此分类」应置灰，并说明「这是最后一个分类」

### 分类合并（指南 3.5.8「需显式迁移分配关系」）

自动化已覆盖「规则（同层级 / 目标不能已归档 / 源不能还有细分用途）、
真的把分配迁过去、同一笔拆给两个分类时金额相加不撞唯一索引、
退款分配改指向且合计不变、被拒时不写任何东西」（`test/category_merge_test.dart`、
`test/category_merge_widget_test.dart`，真机 `database_test.dart` 另有 3 条）。
下面这些靠人手：

- [ ] 新建一个分类「养花」，拿两笔消费归到它 → 打开该分类 → 点「合并到其他分类」
- [ ] 选目标的那一步：列表里**只应该有同层级**的分类
      （一级分类的候选里**不该**出现「咖啡茶饮」这种细分用途），也不该出现它自己
- [ ] 选一个目标 → 确认弹层要说清三件事：账目会改成目标、源分类会被归档、
      **这一步不能撤销**；点「再想想」应什么都不改
- [ ] 确认后：提示应是「N 笔账已归到「X」」，回到分类管理，底部「已归档」里多了那个源分类
- [ ] 进明细 / 月报：那两笔的用途已经变成目标分类，**金额一分没变**，
      两个分类的统计与占比跟着重新汇总
- [ ] 把源分类从「已归档」恢复出来：它能回到选择列表，但里面**已经没有账目**了
- [ ] 拿一笔**同时拆给 A 和 B** 的消费（拆分 → 两个分类各一半）合并 A 到 B：
      结果应是**一项**、金额是两半之和，明细里不再出现「同一笔拆出两项同一分类」
- [ ] 给那笔拆分的消费先挂一笔退款（抵扣到两个拆分项上），再合并：
      退款关联应仍然在、抵扣合计仍等于退款金额，月报净消费不受影响
- [ ] 打开一个**下面还有细分用途**的分类（如「餐饮」）：「合并到其他分类」应置灰，
      并说明「先把它们合并或归档」；把一个一级分类的细分用途全归档后再看，入口应可点
- [ ] 打开一个**已归档**的分类（底部「已归档」区）：它不应出现在任何合并的**目标**列表里

## 十三、导出（PNG / CSV）

- [ ] 月报页「保存我的月度回顾」→ 分享与导出页，**每次进来金额都是隐藏的**
      （上一次打开过开关也不该记住）。
- [ ] 点「导出月度回顾」：**不再弹任何选择界脸**，直接进相册 `Pictures/有数`，
      提示应是「已存到相册：…」。打开相册里那张图确认：**没有金额、没有商户、没有用途名**，
      而且**顶部「∷ 有数」与月份不再重叠**（曾经叠在一起，已修）。
- [ ] 回到分享页打开「展示具体金额」再导出一次：这一张应该有金额，
      且两张图的版式一致（标题、笔数、页脚位置一样）。
- [ ] 「导出明细 CSV」时在系统保存界面**点取消**：不该出现「已保存」提示，也不该报错。
- [ ] 换个主题色再导出：封面色跟着变（背景与文字都换）。
- [ ] 「导出明细 CSV」：用 Excel 或 WPS 打开，中文不乱码（BOM 生效），
      金额列是数字、支出带负号；把商户改成 `=1+1` 再导一次，
      确认单元格显示文本而不是计算结果。
- [ ] 「我的 → 隐私与数据 → 导出本月明细 CSV」：同样能存下来，
      并且页面文案没有把 CSV 说成「备份」。
- [ ] 导入一份**故意坏掉**的账单（比如把几行的时间改成 `9/3`）→ 核对页点
      「需要检查的记录」进异常页 → 点「导出异常明细」：走系统「创建文档」流程，
      存下来的 CSV 用表格软件打开正常，列是 `行号,状态,原因,原始内容`，
      行号对得上原文件，**原始内容里能看到出事那一行的原文**。
      若异常行超过 50 行，页面上会写「还有 N 行没有列出来」，文件里必须一行不少
      （这一条只有真机上能顺手验；自动化测试里已用 55 行守住了）。

## 十四、每月提醒（需要等一天或手改系统时间）

- [ ] 「我的 → 整理提醒」：默认关闭；打开时系统弹出通知权限询问（Android 13+）。
- [ ] **拒绝**权限：开关不能停在「已开启」，要看到说明为什么开不了。
- [ ] 允许权限后：出现「下次提醒约在 …（约 N 天后）」，改日期到明天、时间改成
      几分钟后，把手机时间或系统日期往前调，等到点看通知是否真的到。
- [ ] 收到通知后点开：应**直接落在「整理」页**（不是首页），且是该整理的月份。
      分别验两种情况：应用**在前台**、应用**被切到后台**（热启动走另一条代码路径）。
      还要看**底部导航的高亮是不是「整理」**（曾经停在别的标签上，已修）。
- [ ] 改一次时间再改回来：**不能收到两次**（唯一任务名会替换旧任务）。
- [ ] 关掉开关：不再收到提醒。
- [ ] 重启手机后再看：提醒仍在（WorkManager 会跨重启保留任务）。
- [ ] 到系统设置里把「有数」的通知关掉，回到提醒页：应显示「发不出去」，
      而不是「已开启」；把它打开后状态跟着变。
- [ ] 「清除本地数据」：确认文案提到取消提醒，执行后提醒开关变关闭，
      分类图标图片也确实没了（文件在应用私有目录 category_icons 下）。


## 十五、在线升级（需要先有正式签名的旧版本装着）

⚠️ 换签名等于换应用：调试签名的包与正式签名的包不能互相覆盖。要验这一节，
先 `adb uninstall com.younum.app`，再装**正式签名**的包（命令见 `RELEASE.md`）。
**卸载会清掉本机数据**（示例账本可以重进）。

⚠️ **不要拿线上那份 `v1.1.0+2` 当起点**：它的 release 包与 771a445 之前的所有
release 包一样，**点开就会死**（R8 删掉了 Room 生成的反射构造；进不去也就点不了
「检查更新」）。必须从 tag 重建一份修好的：

```powershell
Set-Location D:\dev\younum
git worktree add D:\dev\younum-v112 'v1.1.0+2'
# 把 app\android\key.properties 与 app\android\app\proguard-rules.pro 拷过去，
# 并在那份 build.gradle.kts 的 release 里加上 isMinifyEnabled = true
#   + proguardFiles(getDefaultProguardFile("proguard-android-optimize.txt"), "proguard-rules.pro")
Set-Location D:\dev\younum-v112\app
flutter build apk --release
adb install -r build\app\outputs\flutter-apk\app-release.apk
```

版本号**不要改**（仍是 2、签名也是同一把），这样它和 1.2.0+3 之间才是真实的覆盖安装。
装好后先造点数据（进示例账本 + 整理一两笔），再发布 1.2.0+3 —— 下一节才有东西可比。

### 已经实测走过一遍的（2026-09-25，1.2.1+4 → 1.2.1+5）

这一趟是在真机上从头走完的，顺序与实际观察记录如下，**这些项不用再验**：

| 步骤 | 实际结果 |
| --- | --- |
| 「我的」底部版本号 | `Version 1.2.1`（真实读自系统，不是写死的） |
| 点「检查更新」 | 显示「发现新版本 1.2.1」，并把 Release 正文当更新说明展示 |
| 点「下载并安装」 | 有进度；下载完**自动**交给系统安装器（`com.miui.packageinstaller`） |
| 系统安装器 | 先弹「有数 正尝试安装应用」→ 点**继续** |
| 再一层 | 「**未查询到此应用的 ICP 备案信息**」→ 点**继续安装**（个人项目没备案，正常） |
| 结果 | 覆盖安装成功：`versionCode 5 / versionName 1.2.1` |
| 升级后启动 | 正常；加密那一步是 `alreadyEncrypted`；「检查更新」变成「已经是最新版本」 |

⚠️ 两条给以后发版用的结论（细节见 `RELEASE.md` 第 5 节）：

* MIUI 把**「退出」放在主按钮位置**、「继续安装」是次要按钮 —— 用户很容易以为装不了。
  发布说明里最好写一句「看到 ICP 备案提示点『继续安装』即可」。
* 更新页把发布说明当**纯文本**显示，所以发布说明别写 Markdown 标记。

### 已经验到的

- [x] **升级之后老账单/分类一条不少**（2026-09-25，设备 412913d4）：
      1.2.1+5 → `1.3.0+6` 用 `adb install -r` 覆盖安装（同签名，正式包）。
      装后 `versionCode` 5→6、`versionName` 1.2.1→1.3.0；冷启动后首页数字
      ¥5,625.86 / 72 笔 / 3 个来源与升级前**完全一致**，整理进度也还是 30%
      （24 笔已整理 / 剩 57 笔）。教训是一句话：**升级前库里得有真实数据**
      —— 上一次就是因为先清了数据，什么都对比不了。
      没覆盖到的：分类图标/自定义主题/导入记录的逐项比对没做（首页读的就是
      同一套库，但没逐页翻）；加密库在升级后能正常打开（否则首页根本没数据），
      但没有用 `run-as` 再看一次文件头（正式包不可 debug，看不了）。

### 还没验到的
- [ ] 网络关掉再点「检查更新」：应显示「连不上网络，稍后再试」，
      **不能**显示「已经是最新版本」（这是最容易骗人的一处）
- [ ] 在系统安装界面点**取消**：应用应提示「取消安装，随时可以再来一次」，
      而不是说成功了
- [ ] 点「忽略这个版本」（在有新版本那一版上试）：副标题变成「已被你忽略」，
      再进这一页时**不该**再弹「发现新版本」；但按钮还在，仍能手动装


## 十六、数据库加密（方案 A）

- [ ] 装好后进「我的 → 隐私与数据」：应写着**数据库已加密**，
      并说明「口令由系统密钥库保管、钥匙丢了无法恢复」
- [ ] 把库拷出来读一眼文件头（应用私有目录，调试包可用 `run-as`）：
      ```powershell
      $adb = "$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe"
      & $adb -s <设备> shell "run-as com.younum.app cat databases/younum.db | od -An -c -N 16"
      ```
      头 16 个字节**不该**是 `S y S Q L i t e   f o r m a t   3`；
      同时 `run-as com.younum.app ls files` 里应有 `younum.database.key`（口令密文）
- [ ] **从旧版升级过来**（先装上一版、导几笔账单，再覆盖安装这一版）：
      打开后账目、分类、图标、导入记录**一条不少**；数据库文件已经从明文
      变成密文；`databases` 目录里不该留下 `younum.db.plain` 残留
- [ ] 杀进程重开：能正常打开（口令从 Keystore 取回），数据还在
- [ ] 把 `younum.db` 拷到电脑上（`adb pull`），用任何 SQLite 工具打开：
      应当打不开、或只能看到乱码 —— 这是「加密真的生效」最直观的一条
- [ ] 「清除本地数据」之后：账本为空，但数据库**仍然能打开**（清数据不等于
      换钥匙，口令不动 —— 换了钥匙会让这个库永久读不出，那是两回事）
- [ ] ⚠️ 不能人工触发但要知道的表现：**卸载重装 / 清了应用数据 / 换手机**之后，
      Keystore 里那把钥匙没了，旧的加密库再也打不开 —— 隐私页写明了
      「卸载或换机前请先导出」，这是加密的必然代价，不是缺陷


## 十七、分享一份账单进来（本轮新增）

这条入口没法自动化到「微信真的分享一次」，所以分两半记：**已经用 adb 走通的**
与**还需要人工确认的**。

已经用 adb 在真机上走通并看过截图（2026-09-25，设备 412913d4，release 包）：

```powershell
$adb = "$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe"
# 1) 把一份账单推到手机上，让 MediaStore 收录它
& $adb -s <设备> push 分享测试.csv /sdcard/Download/分享测试.csv
& $adb -s <设备> shell "content query --uri content://media/external/file --projection _id:_display_name --where _display_name=\'分享测试.csv\'"
# 2) 用它的 _id 造一个带读取授权的 intent（相当于别的应用把文件分享过来）
& $adb -s <设备> shell am start -a android.intent.action.VIEW `
    -d "content://media/external/file/<上一步的 _id>" `
    -t "text/csv" --grant-read-uri-permission -n com.younum.app/.MainActivity
```

- [x] 系统「打开方式」列表里**有有数**（清单里的 MIME 过滤器生效）（2026-09-25）
- [x] 冷启动分享一份 CSV → 直接落到**确认导入**页，数字来自真实解析
      （10 月 / 2 笔 / ¥37.00 / UTF-8）（2026-09-25）
- [x] 分享一份**老式 `.xls`**（真的 OLE2 字节）→ 弹层说清读不了并给出「另存为」，
      不是一页乱码 + 字段映射页（2026-09-25）
- [x] 分享一份**读不了**的文件（`file://` 且应用没有存储权限）→ 弹层原样给出
      `open failed: EACCES (Permission denied)`，**不静默**（2026-09-25）

还需要人工确认的：

- [ ] **从微信里真的分享一份账单过来**：微信 → 打开那份文件 → 右上角 → 用其他应用打开
      → 选有数。看得到有数、且进来就落到确认导入页。⚠️ 这一步没走过：
      adb 造的是同一个原生通道与同一段读取代码，但不是微信那份 UI
- [ ] 从文件管理器里「分享」而不是「用其他应用打开」：`ACTION_SEND` 那条路
      （adb 走的是 `ACTION_VIEW`）
- [ ] 应用**已经在后台**时分享进来（热启动，走 `onNewIntent`）：同样落到确认导入页
- [ ] 分享一份**不是账单**的文件（例如一张图片或一个 zip）：
      要么进字段映射页、要么弹层说清，**不能**静默什么都不做
- [ ] 分享完之后按返回键：应当回到首页（分享这条路没有「选文件」那一页，
      所以栈里只有解析页→核对页，返回就回到首页）


## 十八、启动时的新版本提示（本轮新增）

⚠️ **在真机上看不到它**：线上最新版就是当前安装的 1.2.1+5，没有可提示的版本 ——
「不该弹的时候不弹」这一半已经在真机上验过（冷启动多次，没有任何弹层、启动正常）。
要看弹层，得**先发一个更高版本**，或者临时把 `pubspec.yaml` 的 build number 调低
再装（同签名 `adb install -r -d`；**别对正式数据这么做**）。

- [ ] 装上一版 + 发布一个更高的版本 → 冷启动：应弹「有新版本 x.y.z」
      + 三条路（去更新 / 忽略这个版本 / 以后再说）
- [ ] 点「去更新」→ 进「检查更新」页，能下载、能交给系统安装器
- [ ] 点「以后再说」→ 弹层消失、**不写任何偏好** → 重启应用应当**还会**弹
- [ ] 点「忽略这个版本」→ 同一个版本不再弹（重启也不弹），
      但「我的 → 检查更新」里仍能手动装它；出现更高版本时要**再弹一次**
- [ ] 弹层里的更新说明很长时（Release 正文原文）：整块弹层可滚动，
      「以后再说」不会被挤出屏幕
- [ ] 系统字号调到最大：弹层依然能看全（已在 widget 测试里按 2.0 验过）


## 十九、平板竖屏（2026-09-26，release 包，小米 35c73084）

设备：1800×2880 @400dpi = **720×1152dp**，系统字号 1.0。
装的是 `flutter build apk --release` 的产物，`adb install -r` 覆盖安装
（同签名，数据不动）。逐页看过，确认「内容 560dp 居中、不再拉满」：

- [x] 首页（深色面板左右各留 80dp，与正文同宽）
- [x] 整理页（标题、卡片、按钮、用途网格）
- [x] 月报页、我的页（设置行整行 560dp）
- [x] 分类管理页、分类编辑页（图标网格、输入框、预设图标）
- [x] 用途快捷项页
- [x] 空首页那张插画：收拢成设计宽度，两张票据不再贴到屏幕两边
- [x] 示例账本弹层：量到 560dp（屏幕左右各留 80dp），不再是整屏宽

平板竖屏下的新功能：

- [x] **卡片上的收支方向**：一笔收入（刘旭龙 ¥5,000.00）的卡片徽标是「收入」，
      不再是写死的「支出」（2026-09-26 修，见 `DECISIONS.md` 77 节）
- [x] **详情页字段来自这一笔**：收入那笔是「收入 + 交易性质：收入 + `•••• 5229`」，
      消费那笔是「已归类 · AI + 消费 + `•••• 7457`」（以前面板里每笔都写着
      「商户消费」与 `•••• 0826`）
- [x] **用途快捷项**：按住最右边的手柄拖动 → 顺序真的变了；
      **杀掉进程重开之后顺序还在**（这条同时证明真机上拖得动，
      widget 测试里的拖动是合成手势，不算数）
- [x] **用途快捷项**：满了的时候候选行写「已满」，且点不动
- [x] 「我的」→「退出示例账本」：立刻退出并弹提示条（这条入口**故意**不再二次确认，
      标题与副标题已经把「会怎样」说完了）
- [x] 首页「示例账本」徽标 → 弹层（讲清「退出不删样例数据」）→「继续体验」什么都不改
- [x] 退出之后回到自己的空账本 →「先用示例账单体验」→ **6 笔样例数据与整理进度
      原样都在**（退出是可逆的，这条是它的证据）
- [x] **新建的分类马上能用**（2026-09-26 修）：正常右滑仍然确认（78 → 79，
      随后撤销回 78）；把选中的「宠物」归档后再右滑，弹出「这个用途找不到
      对应的分类，请回到「全部分类」重新选一个」（见 `DECISIONS.md` 78 节）；
      没选用途就右滑，弹「先选一个用途，再右滑确认」。验完把「宠物」恢复、
      撤销那次确认，账目回到 78 / 106

⚠️ **平板上的 adb 注入手法**（和本文件下面那条手机记录不一样，别照错）：

```powershell
$adb = "$env:LOCALAPPDATA\Android\Sdk\platform-tools\adb.exe"

# 同一个坐标有被丢掉的可能，稳妥做法是发两次（本设备实测）
& $adb -s 35c73084 shell input swipe 900 527 900 527 140

# 拖动（长按后移动）必须用 motionevent：input swipe 是一路滑过去的，
# 触发不了「先按住再拖」的手势（ReorderableDragStartListener 要的是这个）
& $adb -s 35c73084 shell "input motionevent DOWN 1540 813; sleep 0.5; input motionevent MOVE 1540 730; sleep 0.4; input motionevent UP 1540 660"
```

没验的：**横屏**（需求方明确说不做）、平板上的系统字号 2.0、平板上的 TalkBack。


## 二十、选择用途与细分用途、emoji 图标（2026-09-26，release 包，小米 35c73084）

设备同上（720×1152dp，release 包，`adb install -r` 覆盖安装）。
入口：整理 → 全部分类 →（选「餐饮」）→「餐饮 · 细分用途」的「新建 / 编辑 ›」。

- [x] **点一下即选中**：点自建的细分用途「waimai2222」→ 底部按钮当场变成
      「使用此分类 · waimai2222」（不用再点一次）
- [x] **直接返回也带值**：按系统返回 → 卡片上就是「确认 · waimai2222」
      （`DECISIONS.md` 79 节的做法，这一步是真机上最容易「静默丢掉」的地方）
- [x] **新建细分用途**：新建页填名字 → 保存 → 列表里立刻出现，且挂在「餐饮」下面
- [x] **改名**：点进去改名 → 保存 → 列表里名字变了，位置（＝ID 与排序）没动
- [x] **删除＝归档**：删除后进「已归档」区，行上有「恢复」，说明写清「不影响已有记录」
- [x] **第二个入口**：我的 → 分类管理 → 餐饮 → 编辑分类图标页里有「细分用途，
      5 个：日常三餐、咖啡茶饮、水果零食、聚餐、买菜」，点进去是同一页
- [x] **内置细分用途不给改名**：列表里内置的那几个显示「内置名称，只能换图标」，
      进去名字框只读并写了原因（自建的才写「点击改名或换图标」）
- [x] **52 个预设图标**：整格渲染正常，没有重叠、没有溢出、没有缺图标
- [x] **emoji 输入框**：存在、能聚焦；填 `abc` 当场报
      「这里只能填 emoji（符号），文字请填在上面的「分类名称」」，预览不变成 `abc`
- [x] **emoji 字形**：提示语「例如 🍜 🐱 ✈️」在真机上正常显示彩色 emoji
- [x] **改完名字保存按钮当场可用**：只改名字、不碰图标，按钮从灰变成可点
      （真机缺陷，`DECISIONS.md` 82 第 1 条）
- [x] **滚到底的「保存」完整可见**：按钮底边离屏幕底还有 100px，不再被手势条压住
      （真机缺陷，`DECISIONS.md` 82 第 2 条）
- [x] 名字不合法时（`waimai%%`）：当场报「名称限 1-12 个文字、数字、空格或短横线」，
      **不关页、不写库**（这条是误打误撞验到的，顺手记下来）

**没验到**（明确记录，不当作已完成）：

- [ ] **真机上输入 emoji 当图标**：`adb shell input text` 只吃 ASCII，
      中文与 emoji 都抛 `NullPointerException`；这台设备也没有 `cmd clipboard`。
      所以「填一个 emoji → 保存 → 到处都画成那个 emoji」这条**在真机上没走通**，
      只有 widget 测试（2 条界面 + 8 条规则）撑着。请拿手机自己点一次再当已验证。
- [ ] **图片图标（从图片选择）**：本轮没碰这条路径，仍是「只能人工验」（见「已知未
      验证」最后一行）。

> 验完已恢复现场：新建的测试细分用途「waimai2222」已删除（进归档区），
> 待选用途改回「餐饮」，没有提交过任何一笔账目。

**同一个包升级到 1.4.0+7（2026-09-26）**：`pubspec.yaml` 改成 `1.4.0+7` 后重新
`flutter build apk --release`，`adb install -r` 覆盖安装设备上已装的 `1.3.0+6`：

- [x] 包本身：`aapt2 dump badging` → `versionCode=7` / `versionName=1.4.0` / label「有数」；
      `apksigner verify --print-certs` → 证书 `CN=UnkownGame`（**不是**调试密钥的
      `CN=Android Debug`），v2 签名
- [x] 包能打开：冷启动后进程存活，各页正常
- [x] 数据一条不少：首页 ¥5,565.46 / 73 笔消费 / 2 个来源 / 已经整理 83 笔（78%）、
      手工补录 7 笔 ¥3,021.11、支付宝 66 笔 ¥2,544.35 —— 与升级前逐项一致
- [x] 应用内读到的版本号跟着变：「我的」页底部从 Version 1.3.0 → **Version 1.4.0**

**最后发布的 build number 改成 `+8`**（`v1.4.0+7` 这个**标签名**被 GitHub 的
「不可变发布」永久占住，推不上去；原因与证据见 `DECISIONS.md` 83 节）：

- [x] 新包 `versionCode=8` / `versionName=1.4.0`，仍是正式签名
- [x] 标签 `v1.4.0+8` 一次推成功（对照：`v1.4.0+7` 推不上去）
- [ ] ⚠️ **`1.4.0+8` 没有在真机上装过**：出包之后平板掉线了（`adb devices` 为空）。
      代码与已验过的 `+7` 那份同源，只有版本号不同；**接上设备后请补一遍
      `adb install -r` 并确认「我的」页读到 Version 1.4.0**。这一条别当已验。

---

## 已知未验证（明确记录，不当作已完成）

| 项目 | 为什么没验证 |
| --- | --- |
| 从微信里真的分享一份账单 | 见第十七节；adb 走的是同一条原生通道，但不是微信那份 UI |
| 启动时的新版本提示长什么样 | 线上最新版 = 当前安装版，没什么可提示的；逻辑由 8 条界面用例守着，见第十八节 |
| 空首页的装饰插画在系统字号 2.0 下溢出 **254px / 160px** | 本轮加「大字号不溢出」用例时撞出来的**既有**问题（`start_screens.dart` 的 `_MiniReceipt`，固定 152×172 的盒子装不下放大后的字）。2026-09-26 又用探针复测过一次，数字没变（与平板限宽那次改动无关），**仍未修**：它要改的是插画的高度策略，而仓库里明确写过「不把容器的限制转嫁给用户」（见 `text_scale_widget_test.dart` 开头），所以是一次取舍，留给需求方定 |
| 用 adb 点真机界面（自动化手段） | 分设备：**412913d4（手机，小米）屏蔽了注入的点击** —— `input tap` 与 `input swipe x y x y 140` 都无效，只有 `input keyevent` 有效（要开「USB 调试（安全设置）」）。**35c73084（平板）可以**，但同一坐标偶尔丢一次，发两次更稳；拖动要用 `input motionevent DOWN/MOVE/UP`（见第十九节）。跨屏幕坐标要按 **1.667 倍**换算（截图 1080 宽 ↔ 设备 1800 像素） |

| 项目 | 为什么没验证 |
| --- | --- |
| 跨来源疑似重复在真机上的逐组确认 | 判定规则与「疑似行照旧计入」已在 `test/import_cross_source_test.dart` 与真账单上验过；但核对页上「逐组选保留一笔 / 保留两笔」这段交互要靠人手点 |
| 陌生表头走映射页手工指列 | 解析与提交已用真文件验过（认不出表头时如实返回 `ImportMappingRequired`，带映射再暂存即可）；映射页本身的点按路径要人工走。银行卡账单已经不走这条路了 —— 它有平台专用适配器 |
| 导出在真机上的保存流程 | 内容与编排已有单测（含「隐藏金额时清单里没有金额」）与引擎测试（PNG 真的生成、开关真的改变字节），界面接线有部件测试；但**系统「创建文档」界面本身**（选位置、取消、磁盘满）只能人工走，见第十三节 |
| 用户真在系统选择器里点选文件 | 需要人手点，见上面第一节 |
| 导入异常明细导出的系统保存界面 | 完整清单、列名、行号与防注入都有单测与部件测试（55 行坏行守「导的比列的多」），但系统「创建文档」界面本身只能人工走，见第十三节 |
| 六套主题下的导入页面与引导页 | 见第四节与第七节 |
| 本机启动白屏（Impeller） | 已定性为设备端图形问题，不是应用缺陷；未确认重启手机后是否恢复，见第九节 |
| 引导页的读屏 / 横屏 / 大字号 | 只在 widget 测试里走过语义，真机上没验，见第七节 |
| 分类图片图标的选图与编辑器接线 | 数据层与整条处理链路（解码 → 居中裁方 → 落私有目录 → 落库 → 重启仍在 → 恢复默认清理）已在单测与真机验过；但「编辑器里选图 → 预览 → 保存」这条界面路径没有自动化覆盖：widget 测试里做真实图片解码必须 `runAsync`，与 `pumpAndSettle` 互相等待，反复尝试都卡到超时。所以它只能人工验（见上一节），**不要当成已验证** |

> 微信真实账单已于 2026-09-25 验过（表头第 18 行、6 笔、支出 3022.40 元 /
> 收入 1.76 元与文件自带汇总一致），因此从本表移出。


