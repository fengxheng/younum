# ---------------------------------------------------------------------------
# 设计稿（原型的 HTML / CSS / JS 与三份说明文档）
#
# 需求方选择不把原型纳入版本控制 —— 它仍然是工作区里的参考，只是不进 git 历史。
# 这里排除的是**整个目录**，与之前逐文件列出的效果完全一致，
# 但以后新增的设计文件也会一并被排除。
#
# 如果想让它也进入版本控制（比如当作备份），把下面这一行删掉即可。
# ---------------------------------------------------------------------------
设计稿/

# ---------------------------------------------------------------------------
# Flutter / Dart
# ---------------------------------------------------------------------------
app/.dart_tool/
app/.pub/
app/.pub-cache/
app/.flutter-plugins
app/.flutter-plugins-dependencies
app/build/

# ---------------------------------------------------------------------------
# Android / Gradle
# ---------------------------------------------------------------------------
app/android/.gradle/
app/android/.cxx/
app/android/local.properties
app/android/key.properties
app/android/app/debug/
app/android/app/profile/
app/android/app/release/

# 签名密钥绝不入库（实现指南 2.3.9）。
*.jks
*.keystore

# ---------------------------------------------------------------------------
# IDE 与系统
# ---------------------------------------------------------------------------
**/.idea/
**/*.iml
**/.vscode/*
!**/.vscode/settings.json
Thumbs.db
.DS_Store
desktop.ini

# ---------------------------------------------------------------------------
# 构建与日志残留
# ---------------------------------------------------------------------------
*.log
/app/test/账单
/keystore
